export {
  THEME_STORAGE_KEY,
  applyInitialTheme,
  applyThemeToHtml,
  createImprensa,
  getStoredTheme,
  mountOrHydrate,
  setStoredTheme,
  shiki,
  shikiThemes,
} from "./client-runtime";

export { createPrerender, type ImprensaPrerenderOptions } from "./prerender-core";
