{
  "name": "imprensa",
  "private": true,
  "workspaces": [
    "packages/*",
    "templates/*"
  ],
  "type": "module",
  "scripts": {
    "build": "bun run --filter './packages/*' build",
    "lint": "oxlint",
    "fmt": "oxfmt",
    "test": "bun run --filter './packages/*' test"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "oxfmt": "^0.58.0",
    "oxlint": "^1.73.0",
    "typescript": "^6.0.3"
  }
}
